const defaultState = {
    input: 'Write something',
    listData: [
        '早上8点开会redux',
        '9点需求讨论',
        '晚上5点对接 代码 review'
    ]
}
export default (state = defaultState, action ) => {
    return state
}